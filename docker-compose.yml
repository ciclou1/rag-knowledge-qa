version: "3.9"

services:
  api:
    build: ./api
    container_name: rag-api
    ports:
      - "8000:8000"
    env_file:
      - ./api/.env
    restart: unless-stopped

  # 本地开发时的 Supabase 替代（可选，生产环境用托管 Supabase）
  # supabase:
  #   image: supabase/postgres:15.1.0.147
  #   ...
